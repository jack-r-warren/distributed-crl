fun main(args: Array<String>) = ClientRunner { otherServers: Collection<NetworkLocation> ->
  // Just instantiate a Runnable (probably an implementation of the Observer interface) here,
  // and the client runner will handle the rest
  TODO()
}.main(args)